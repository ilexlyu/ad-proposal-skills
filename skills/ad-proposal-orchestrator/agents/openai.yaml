interface:
  display_name: "广告提案总控"
  short_description: "动态编排并执行广告提案全流程"
  default_prompt: "Use $ad-proposal-orchestrator to take this brief from intake to a validated client-ready proposal. Route dynamically, execute available stages, preserve evidence and assumptions, and stop only for material blockers."
policy:
  allow_implicit_invocation: true
